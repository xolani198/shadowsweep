# Server-side secrets only. Never use the NEXT_PUBLIC_ prefix for keys —
# NEXT_PUBLIC_ variables are inlined into the client bundle and become public.

# HMAC key for session cookie verification (32+ random bytes, e.g. `openssl rand -base64 32`)
SESSION_SECRET=
